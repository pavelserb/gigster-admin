services:
  - type: web
    name: gigster-admin
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: FTP_HOST
        value: somos.ftp.tools
      - key: FTP_USER
        value: somos_cursor
      - key: FTP_PASSWORD
        value: Pr6LUx9h45
      - key: FTP_PORT
        value: "21"
      - key: FTP_REMOTE_PATH
        value: .
    healthCheckPath: /admin/api/status
    autoDeploy: true
